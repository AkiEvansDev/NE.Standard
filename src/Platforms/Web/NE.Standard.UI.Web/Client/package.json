{
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.ts",
    "build": "npm run typecheck && npm test && vite build --clearScreen false --logLevel error && vite build -c vite.boot.config.ts --clearScreen false --logLevel error"
  },
  "dependencies": {
    "@microsoft/signalr": "10.0.11"
  },
  "devDependencies": {
    "@types/node": "26.4.1",
    "@vitejs/plugin-basic-ssl": "2.3.0",
    "less": "4.9.0",
    "typescript": "7.0.2",
    "vite": "8.2.2"
  }
}
