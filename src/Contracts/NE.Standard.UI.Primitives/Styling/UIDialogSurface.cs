namespace NE.Standard.UI.Primitives.Styling;

/// <summary>
/// What a dialog's panel is made of.
/// </summary>
public enum UIDialogSurface
{
    /// <summary>A raised card, lifted off the page it covers.</summary>
    Card,

    /// <summary>The page's own background inside a border — a dialog that reads as a screen rather than a card.</summary>
    Background
}
