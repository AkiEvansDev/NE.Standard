namespace NE.Standard.UI.Primitives.Styling;

/// <summary>
/// Where a field's caption stands.
/// </summary>
public enum UIInputTitlePlacement
{
    /// <summary>On a line of its own above the field.</summary>
    Top = 0,

    /// <summary>Inside the field's own box at its leading edge, with the value read from the trailing one.</summary>
    Inside = 1,
}
